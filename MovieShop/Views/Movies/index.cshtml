@model IEnumerable<ApplicationCore.Entities.MovieCardModel>

<div class="row">
    @foreach (var movie in Model)
    {
        <div class="col-md-4">
            @Html.Partial("_MovieCard", movie)
        </div>
    }
</div>
